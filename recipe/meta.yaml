package:
  name: otsubsetinverse
  version: 1.1

source:
  path: ../

build:
  number: 1

requirements:
  build:
    - gcc
    - toolchain
    - python
    - cmake  # [not win]
    - swig  # [not win]
    - openturns 1.11  # [not win]

  run:
    - python
    - openturns

test:
  imports:
    - otsubsetinverse

about:
  home: http://openturns.org/
  license: LGPLv3
  summary: Subset inverse simulation

extra:
  maintainers:
   - adumas